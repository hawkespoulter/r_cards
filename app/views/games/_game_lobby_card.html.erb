<div class="card flex-container">
  <div>Name of game:<%= game.name %></div>
  <div>players: Sam, Hawkes, Sicily, Normandy</div>
  <button>Join</button>
  <%= button_to 'Delete', game_path(game), method: :delete, data: { confirm: 'Are you sure?' }, class: 'danger' %>
</div>